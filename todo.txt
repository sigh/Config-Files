.bashrc
    + Fix lsd function so that it is more robust (no grep)

.vimrc
    + display pwd in empty cmd area
    + map C-O to go forward, C-o to co backward
    + mouse support for MBE
    + shortcut deleting buffer
    + svn plugin
    + Alt+num goes to buffer
